import { MyHostDirective } from './my-host.directive';

describe('MyHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MyHostDirective();
    expect(directive).toBeTruthy();
  });
});
